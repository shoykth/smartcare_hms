import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF1976D2);
  static const Color secondary = Color(0xFF42A5F5);
  static const Color background = Color(0xFFF5F5F5);
  static const Color surface = Colors.white;
  static const Color error = Color(0xFFD32F2F);
  static const Color success = Color(0xFF388E3C);
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
}

class AppStyles {
  static const EdgeInsets screenPadding = EdgeInsets.all(24.0);
  static const BorderRadius cardBorderRadius = BorderRadius.all(Radius.circular(16.0));
  static const double buttonHeight = 56.0;
  static const double inputFieldHeight = 56.0;
}

class AppText {
  static const String appName = 'SmartCare HMS';
  static const String loginTitle = 'Welcome Back';
  static const String loginSubtitle = 'Sign in to continue';
  static const String registerTitle = 'Create Account';
  static const String registerSubtitle = 'Sign up to get started';
}

